// Arrays

// Given an array of numbers, return only the even ones using filter
// Given an array of names, return them all in uppercase using map
// Given an array of prices, calculate the total using reduce
// Given an array of objects, find the one with a specific name using find
// Check if any item in a cart array is out of stock using some

// Objects

// Create a student object with name, age, and grade properties — then update the grade
// Loop through an object and print each key and value using for...in
// Given an object of prices, use Object.keys() to list all items and Object.values() to get the total
// Write a function that takes a person object and returns a formatted sentence using their properties

// Loops

// Print numbers 1 to 20, but print "Fizz" for multiples of 3 and "Buzz" for multiples of 5 — this is the classic FizzBuzz
// Loop through an array of scores and count how many are above 50
// Use for...of to loop through a string and count how many vowels it has
// Use while to keep generating a random number until it hits 7, counting how many tries it took

// Combining all three

// Given an array of student objects each with a name and score, use filter to get those who passed, map to extract just their names, and reduce to calculate the class average
// Build a small inventory system — an array of product objects, with functions to add a product, remove by name, and calculate total stock value

